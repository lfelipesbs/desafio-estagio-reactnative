import { StyleSheet } from "react-native";
import { corDeFundo } from "../../constants/css";

export const homeStyles = StyleSheet.create({
    tweet: {
        flex: 1,
        backgroundColor: corDeFundo
    }
})